name: Build AOSP Module

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo dpkg --add-architecture i386
        sudo apt-get update
        sudo apt-get install -y git-core gnupg flex bison gperf build-essential \
          zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 \
          libncurses5-dev libncursesw5-dev libncurses5-dev:i386 \
          x11proto-core-dev libx11-dev lib32z-dev \
          libgl1-mesa-dev libxml2-utils xsltproc unzip python3

        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
        chmod a+x ~/repo
        sudo mv ~/repo /usr/local/bin/

    - name: Clone AOSP
      run: |
        mkdir aosp && cd aosp
        repo init -u https://android.googlesource.com/platform/manifest -b master
        repo sync -j$(nproc)

    - name: Setup environment
      run: |
        cd aosp
        source build/envsetup.sh
        lunch aosp_arm-eng

    - name: Build module
      run: |
        cd aosp
        # Cambia "frameworks-base" por el módulo que quieras compilar
        make frameworks-base -j$(nproc)
